# Read the Docs build requirements for EpiLearn 0.1.0.
#
# IMPORTANT: this file must list every dependency that a bare `import epilearn`
# needs.  Sphinx autodoc imports the package to read its docstrings; when an
# import fails, autodoc only emits a *warning* and the published API reference
# comes out silently half-empty (that is how the 0.0.x docs lost every task and
# visualization signature -- torch_geometric was missing here).  docs/conf.py now
# hard-fails the build if the package cannot be imported, so a missing entry in
# this file breaks the build loudly instead of publishing an incomplete page.
#
# Keep the second block in sync with `[project].dependencies` in pyproject.toml.
# torch itself is installed first, from docs/requirements_torch.txt.
#
# NOT listed on purpose:
#   * epilearn -- conf.py puts the checkout on sys.path; installing a release
#     from PyPI would document that release instead of this branch.
#   * chronos-forecasting / uni2ts / momentfm / timesfm -- optional foundation
#     backends, mocked via autodoc_mock_imports in conf.py.
#   * plotly -- only needed by Forecast.plot_preds(interactive=True) at runtime.

# --- Sphinx toolchain ---
sphinx>=7.0
sphinx-rtd-theme
sphinx-autobuild
docutils==0.19.0

# --- epilearn hard dependencies (mirror of pyproject.toml [project].dependencies) ---
torch_geometric>=2.3
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
matplotlib==3.9.0
seaborn==0.13.2
networkx==3.2.1
scikit-learn==1.5.0
statsmodels==0.14.2
optuna>=3.0
psutil>=5.9
tqdm==4.66.4
einops==0.8.0
fastdtw==0.3.4
PyYAML>=6.0
