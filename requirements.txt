# Runtime dependencies for EpiLearn.
#
# This mirrors [project.dependencies] in pyproject.toml -- prefer
# `pip install .` (or `pip install epilearn`), which reads that file.
#
# torch and torch_geometric are listed without a build variant: for a CUDA
# build follow https://pytorch.org/ instead of installing from here.

torch>=1.13
torch_geometric>=2.3
numpy>=1.24
scipy>=1.9
pandas>=1.5
matplotlib>=3.6
seaborn>=0.12
networkx>=2.8
scikit-learn>=1.1
statsmodels>=0.14
optuna>=3.0
psutil>=5.9
tqdm>=4.64
einops>=0.6
fastdtw>=0.3
PyYAML>=6.0

# Optional: foundation-model backends, one extra per backend, e.g.
#   pip install epilearn[chronos]      (also [moirai], [moment], [timesfm])
# Their upstream pins conflict with each other -- use one per environment.
# chronos-forecasting
# uni2ts
# momentfm
# timesfm

# Optional: interactive plots (pip install epilearn[plot])
# plotly>=5.0
